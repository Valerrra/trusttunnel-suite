# Config Bridge

Единый слой интеграции конфигов между WebUI, desktop и Android.

Планируемая ответственность:

- parsing `tt://`;
- import/export profile model;
- adapters для TOML и локальных конфигов;
- payload preview перед импортом;
- QR/deep-link compatibility.
